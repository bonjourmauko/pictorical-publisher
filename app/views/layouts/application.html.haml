!!!
%html
  %head
    %title= yield :title
    = stylesheets "application"
    = javascript_include_tag :defaults
    -#= include_fancy_box
    = yield :head
    = csrf_meta_tag
  %body
    -if user_signed_in? && current_user.admin?
      #admin-bar
        .x Hello, Master
        .x
          %strong
            = link_to "Invites", invitations_path
          = link_to "new", new_invitation_path
          = link_to "red'md", invitations_path + "?redeemed=yes"
          = link_to "not-red'md", invitations_path + "?redeemed=no"
        .x
          %strong
            = link_to "Users", users_path
        .x
          %strong
            = link_to "Books", books_path
          = link_to "active", books_path + "?status=active"
          = link_to "review", books_path + "?status=review"
          = link_to "published", books_path + "?status=published"
          = link_to "destroyed", books_path + "?status=destroyed"
        .x
          %strong
            = link_to "Texts", texts_path
          = link_to "new", new_text_path
          = link_to "trash", trashes_path

        .x
          %strong
            = link_to "Authors", authors_path
          = link_to "new", new_author_path
      
      
      
    
    #header
      = yield :navbar
      #artist_session
        - if user_signed_in?
          = "Hello #{current_user.first_name}"
          = link_to "sign out",  destroy_user_session_path  
          - unless current_user.active_book.nil? || current_user.active_book.text.nil?
            ·  
            = "You are illustrating " 
            = link_to "#{current_user.active_book.title}", :controller => "books", :action => "edit", :id => current_user.active_book.id
          - if current_user.has_books_under_review?
            = "We are reviewing #{current_user.book_under_review.text.title}"
          
          
          .session
          
    = yield
